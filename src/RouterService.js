const exportsRoute = (function(myCustomRoutes) {
  //let express = require("express");
  //let router = express.Router();
  //let methods = Object.keys(myCustomRoutes); // getting methods ('get', 'post'... etc)
  console.log("exportsRoute");
})();

module.exports = {
  exportsRoute
};
